apiVersion: v1
kind: Service
metadata:
  name: sandbox-api
  namespace: stack-auth-sandbox
  labels:
    app: sandbox-api
spec:
  type: ClusterIP
  selector:
    app: sandbox-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
